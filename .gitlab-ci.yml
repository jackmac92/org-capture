include:
  - project: "quickstart-templates/ci-templates"
    ref: master
    file: "node/deno.yml"

vet:
  tags:
    - jmccown-user
semantic-releaser:
  stage: .post
  tags:
    - jmccown-user
  script:
    - npx semantic-release
